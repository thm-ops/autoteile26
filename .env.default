# NextJS configuration
NEXT_HOST=0.0.0.0
NEXT_PORT=3000

# PostgreSQL
POSTGRES_VERSION=16-alpine
POSTGRES_USER=autoteile26
POSTGRES_PASSWORD=changeme_in_local_env
POSTGRES_DB=autoteile26
POSTGRES_PORT=5432

# Volume names
DB_VOLUME=autoteile26-pgdata

# Connection string 
DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@db-dev:${POSTGRES_PORT}/${POSTGRES_DB}