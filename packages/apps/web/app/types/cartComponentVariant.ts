/**
 * Type for the variant of the cart components. It can be either 'page' or 'drawer'.
 * It is used to differentiate between the cart page and the cart drawer.
 */
export type CartComponentVariant = 'page' | 'drawer';
